[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "betbot-backend"
version = "0.1.0"
description = "Backend package for BetBot"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["."]
