# Docker Compose PostgreSQL development configuration. All values are safe
# placeholders and must be replaced locally; never commit the resulting `.env`.
POSTGRES_DB=betbot
POSTGRES_USER=betbot
POSTGRES_PASSWORD=change-me
POSTGRES_PORT=5432
